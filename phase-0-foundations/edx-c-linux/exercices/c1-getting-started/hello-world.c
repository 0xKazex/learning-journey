#include <stdio.h>           // It's the main library
int main(void) {             // main function, where all the program run
  printf("Hello, World!\n"); // print something in the terminal
  return 0;                  // Say that it run correctly
}
